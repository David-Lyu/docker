sudo: required
services: docker

before_install:
  - docker build -t dlyu/front-end -f ./fullProject/front-end/Dockerfile.dev ./fullProject/front-end/

script:
  - docker run -e CI=true dlyu/front-end npm run test
